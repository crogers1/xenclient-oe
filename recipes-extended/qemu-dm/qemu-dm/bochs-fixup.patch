--- a/configure
+++ b/configure
@@ -33,7 +33,7 @@ then
 
     mkdir build
     touch $MARKER
-
+    cp ../config-disable.mak ./build/
     cat > GNUmakefile <<'EOF'
 # This file is auto-generated by configure to support in-source tree
 # 'make' command invocation
--- a/meson.build
+++ b/meson.build
@@ -1958,6 +1958,12 @@ foreach target : target_dirs
 
   if target.endswith('-softmmu')
     config_input = meson.get_external_property(target, 'default')
+    extraconf = []
+    config_devices_disable_mak = 'build/config-disable.mak'
+    config_devices_disable = keyval.load(config_devices_disable_mak)
+    foreach k, v: config_devices_disable
+      extraconf += k + '=n'
+    endforeach
     config_devices_mak = target + '-config-devices.mak'
     config_devices_mak = configure_file(
       input: ['configs/devices' / target / config_input + '.mak', 'Kconfig'],
@@ -1967,14 +1973,16 @@ foreach target : target_dirs
       command: [minikconf,
                 get_option('default_devices') ? '--defconfig' : '--allnoconfig',
                 config_devices_mak, '@DEPFILE@', '@INPUT@',
-                host_kconfig, accel_kconfig,
-                'CONFIG_' + config_target['TARGET_ARCH'].to_upper() + '=y'])
-
+                host_kconfig, accel_kconfig, 
+                'CONFIG_' + config_target['TARGET_ARCH'].to_upper() + '=y', extraconf])
     config_devices_data = configuration_data()
     config_devices = keyval.load(config_devices_mak)
     foreach k, v: config_devices
       config_devices_data.set(k, 1)
     endforeach
+    #foreach k, v: config_devices_disable
+    #  config_devices_data.set(k, 0)
+    #endforeach
     config_devices_mak_list += config_devices_mak
     config_devices_h += {target: configure_file(output: target + '-config-devices.h',
                                                 configuration: config_devices_data)}
--- a/scripts/minikconf.py
+++ b/scripts/minikconf.py
@@ -111,11 +111,11 @@ class KconfigData:
             return not (self.value is None)
         def set_value(self, val, clause):
             self.clauses_for_var.append(clause)
-            if self.has_value() and self.value != val:
-                print("The following clauses were found for " + self.name)
-                for i in self.clauses_for_var:
-                    print("    " + str(i), file=sys.stderr)
-                raise KconfigDataError('contradiction between clauses when setting %s' % self)
+            #if self.has_value() and self.value != val:
+            #    print("The following clauses were found for " + self.name)
+            #    for i in self.clauses_for_var:
+            #        print("    " + str(i), file=sys.stderr)
+            #    raise KconfigDataError('contradiction between clauses when setting %s' % self)
             debug_print("=> %s is now %s" % (self.name, val))
             self.value = val
 
--- a/hw/display/vga-pci.c
+++ b/hw/display/vga-pci.c
@@ -265,6 +265,11 @@ static void pci_std_vga_realize(PCIDevic
 
         pci_register_bar(&d->dev, 2, PCI_BASE_ADDRESS_SPACE_MEMORY, &d->mmio);
     }
+
+	if (!dev->rom_bar) {
+        /* compatibility with pc-0.13 and older */
+        vga_init_vbe(s, OBJECT(dev), pci_address_space(dev));
+    }
 }
 
 static void pci_secondary_vga_realize(PCIDevice *dev, Error **errp)
--- a/hw/display/vga.c
+++ b/hw/display/vga.c
@@ -2303,3 +2303,18 @@ void vga_init(VGACommonState *s, Object
         portio_list_add(&s->vbe_port_list, address_space_io, 0x1ce);
     }
 }
+
+void vga_init_vbe(VGACommonState *s, Object *obj, MemoryRegion *system_memory)
+{
+    /* With pc-0.12 and below we map both the PCI BAR and the fixed VBE region,
+     * so use an alias to avoid double-mapping the same region.
+     */
+    memory_region_init_alias(&s->vram_vbe, obj, "vram.vbe",
+                             &s->vram, 0, memory_region_size(&s->vram));
+    /* XXX: use optimized standard vga accesses */
+    memory_region_add_subregion(system_memory,
+                                VBE_DISPI_LFB_PHYSICAL_ADDRESS,
+                                &s->vram_vbe);
+    s->vbe_mapped = 1;
+}
+
--- a/hw/display/vga_int.h
+++ b/hw/display/vga_int.h
@@ -60,6 +60,7 @@ typedef struct VGACommonState {
     MemoryRegion *legacy_address_space;
     uint8_t *vram_ptr;
     MemoryRegion vram;
+	MemoryRegion vram_vbe;
     uint32_t vram_size;
     uint32_t vram_size_mb; /* property */
     uint32_t vbe_size;
@@ -105,6 +106,7 @@ typedef struct VGACommonState {
     uint32_t vbe_start_addr;
     uint32_t vbe_line_offset;
     uint32_t vbe_bank_mask;
+	int vbe_mapped;
     /* display refresh support */
     QemuConsole *con;
     uint32_t font_offsets[2];
@@ -176,6 +178,7 @@ void vga_invalidate_scanlines(VGACommonS
 
 int vga_ioport_invalid(VGACommonState *s, uint32_t addr);
 
+void vga_init_vbe(VGACommonState *s, Object *obj, MemoryRegion *address_space);
 uint32_t vbe_ioport_read_data(void *opaque, uint32_t addr);
 void vbe_ioport_write_index(void *opaque, uint32_t addr, uint32_t val);
 void vbe_ioport_write_data(void *opaque, uint32_t addr, uint32_t val);
